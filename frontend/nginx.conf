server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Tüm istekleri index.html'e yönlendir (React Router için kritik)
        try_files $uri $uri/ /index.html;
    }
}